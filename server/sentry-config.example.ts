/**
 * Exemplo de configuração do Sentry
 * 
 * Para habilitar o Sentry:
 * 
 * 1. Instale o pacote:
 *    npm install @sentry/node
 * 
 * 2. Configure a variável de ambiente:
 *    SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id
 * 
 * 3. O logger automaticamente começará a enviar erros para o Sentry
 * 
 * Opcionalmente, você pode configurar:
 * - SENTRY_ENVIRONMENT=production|staging|development
 * - SENTRY_TRACES_SAMPLE_RATE=0.1 (0.0 a 1.0)
 */

// O código de integração já está no logger.ts
// Este arquivo é apenas documentação

export {};

